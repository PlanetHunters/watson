[tox]
envlist = py{37,38}

[testenv]
deps=
    numpy
    pytest
    setuptools
    wheel
    Cython
whitelist_externals = pytest
    /bin/rm
commands =
    pytest -v -x patoso/tests/
    rm .tox/{envname} -R
